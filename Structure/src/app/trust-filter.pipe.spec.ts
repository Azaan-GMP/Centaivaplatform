import { TrustFilterPipe } from './trust-filter.pipe';

describe('TrustFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TrustFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
